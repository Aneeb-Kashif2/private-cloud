# Named tunnel configuration for the EC2 host.
tunnel: replace-with-cloudflare-tunnel-id
credentials-file: /etc/cloudflared/replace-with-tunnel-id.json
ingress:
  - hostname: app.example.invalid
    service: http://127.0.0.1:8080
  - service: http_status:404
