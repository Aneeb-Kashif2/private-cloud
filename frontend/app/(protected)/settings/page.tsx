import { StorageSummary } from "@/components/storage-summary";
export default function SettingsPage(){return <div className="mx-auto max-w-3xl"><p className="cyber-kicker">SYSTEM CONFIG</p><h1 className="mt-2 text-2xl font-semibold">Node settings</h1><p className="mt-2 text-xs text-[var(--muted)]">Account telemetry and security parameters</p><div className="mt-8"><StorageSummary/></div><section className="cyber-panel mt-6 p-5"><div className="flex items-center justify-between border-b border-[var(--line)] pb-3"><h2 className="text-sm font-semibold">PRIVACY PROTOCOL</h2><span className="text-[9px] text-[var(--accent)]">ENFORCED</span></div><p className="mt-4 text-xs leading-6 text-[var(--muted)]">Objects remain private and are delivered through temporary signed links. Bucket credentials are isolated inside the backend node and never transmitted to this browser.</p></section></div>}
