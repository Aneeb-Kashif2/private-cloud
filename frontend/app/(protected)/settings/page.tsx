import { LockKeyhole, ShieldCheck } from "lucide-react";
import { StorageSummary } from "@/components/storage-summary";
export default function SettingsPage(){return <div className="mx-auto max-w-4xl"><p className="cyber-kicker">Workspace settings</p><h1 className="mt-2 text-3xl font-bold tracking-tight">Your cloud, your rules.</h1><p className="mt-3 text-sm text-[var(--muted)]">Review your storage allocation and privacy posture.</p><div className="mt-8"><StorageSummary/></div><section className="cyber-panel mt-5 p-5 sm:p-6"><div className="flex items-start gap-4"><div className="grid size-10 shrink-0 place-items-center rounded-xl bg-[var(--glow)] text-[var(--accent)]"><LockKeyhole size={20}/></div><div className="flex-1"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-base font-bold">Private storage</h2><p className="mt-1 text-xs text-[var(--muted)]">Your account is the only key to this workspace.</p></div><span className="flex items-center gap-1.5 rounded-full bg-[color-mix(in_srgb,var(--accent-2)_12%,transparent)] px-2.5 py-1 text-[10px] font-bold text-[var(--accent-2)]"><ShieldCheck size={13}/> Protected</span></div><p className="mt-5 max-w-2xl text-sm leading-6 text-[var(--muted)]">Objects remain private and are delivered through temporary signed links. Bucket credentials are isolated inside the backend node and never transmitted to this browser.</p></div></div></section></div>}
